import React from "react";
import styles from "./styles.module.scss";

const View = () => {
    return (
        <div className={styles.wrapper}>
            <button className={styles.wrapper__button}>Найти</button>
        </div>
    );
};

export default View;
